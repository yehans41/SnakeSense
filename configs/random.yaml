# Configuration for Random agent baseline

agent:
  type: 'random'
  seed: 42

environment:
  board_size: 10
  state_type: 'feature'
  deterministic: false
  seed: 42

rewards:
  food: 1.0
  death: -1.0
  step: -0.01

evaluation:
  episodes: 100
  render: false
